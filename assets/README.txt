### INFO ###

# What's the install.ps1 for?
- To basically just install the program for your computer (all it does is move it to $USERPROFILE/systemup and schedules a task to run on startup.)

# How do I do so?
- Run the powershell install.ps1 file as Administrator in the same directory as systemup.exe.